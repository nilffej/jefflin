import { EXPERIENCE_SECTION } from '../../common/sections';
import { SectionContainer } from '../../common/styled';

const ExperienceSection: React.FC = () => {
    return <SectionContainer id={EXPERIENCE_SECTION}>Experience Section</SectionContainer>;
};

export default ExperienceSection;
